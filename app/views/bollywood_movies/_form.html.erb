<%= form_for(@bollywood_movie) do |f| %>
  <% if @bollywood_movie.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bollywood_movie.errors.count, "error") %> prohibited this bollywood_movie from being saved:</h2>

      <ul>
      <% @bollywood_movie.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
